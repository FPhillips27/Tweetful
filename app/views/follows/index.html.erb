<%= render :partial => 'shared/left_nav_notweet' %>
<h4 style="margin-left: 1em">Who Else is on Tweetful?</h4>
<p style="margin-left: 1.5em">Tweetful accounts in alpabetical order.</p>
<ol>
  <% @users.each do |user| %>
    <li>
      <%= content_tag :strong, user.name, :class => 'pull-left text-middle' %>
      <%= content_tag :span, "@"+user.username, :class => 'text-muted pad-10 text-middle' %>
      <%= follow_link(user) %>
    </li>
    <li><hr class="flat"></li>
  <% end %>
</ol>